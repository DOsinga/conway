{
  "name": "conway",
  "version": "0.1.0",
  "description": "Animation of an cellular automata",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w"
  },
  "homepage": "https://github.com/DOsinga/conway",
  "dependencies": {
    "@types/p5": "^0.9.1",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.7"
  }
}
